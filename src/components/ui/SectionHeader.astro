---
interface Props {
    label?: string;
    title: string;
    subtitle?: string;
    align?: "left" | "center";
    dark?: boolean;
    class?: string;
    /** Heading level for the title — pass "h1" when this is the page title (#16). */
    as?: "h1" | "h2";
}

const {
    label,
    title,
    subtitle,
    align = "center",
    dark = false,
    class: className,
    as: Tag = "h2",
} = Astro.props as Props;

const alignClasses = align === "center" ? "text-center mx-auto" : "text-left";
const colorClasses = dark ? "text-highlight" : "text-dark";
---

<div class:list={["mb-12 max-w-3xl", alignClasses, className]}>
    {
        label && (
            <span
                class:list={[
                    "mb-3 block text-sm font-semibold uppercase tracking-widest",
                    dark ? "text-accent" : "text-primary",
                ]}
            >
                {label}
            </span>
        )
    }
    <Tag
        class:list={[
            "font-heading text-3xl font-bold md:text-4xl lg:text-5xl",
            colorClasses,
        ]}
    >
        {title}
    </Tag>
    {
        subtitle && (
            <p
                class:list={[
                    "mt-4 text-lg",
                    dark ? "text-highlight/70" : "text-dark/60",
                ]}
            >
                {subtitle}
            </p>
        )
    }
</div>
