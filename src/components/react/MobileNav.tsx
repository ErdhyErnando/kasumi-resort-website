/**
 * MobileNav — Full-screen overlay mobile navigation.
 * React island component, loaded with client:load on mobile.
 * Stub — will be fully implemented in a future sprint.
 */
export default function MobileNav() {
    return null; // Navigation handled via Navbar.astro inline for now
}
